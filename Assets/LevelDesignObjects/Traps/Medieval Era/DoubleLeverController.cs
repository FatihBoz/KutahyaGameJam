using UnityEngine;

public class DoubleLeverController : MonoBehaviour
{
    [SerializeField] private DoubleLever lever1;
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
